/// Copiez ce fichier en `tunnel_api_url.dart` ou lancez `scripts/start_ngrok.ps1`
/// qui le génère automatiquement avec l’URL ngrok du jour.
library;

/// URL HTTPS ngrok vers le backend local (…/api/v1). Vide = tunnel désactivé.
const kTunnelApiUrl = 'https://VOTRE-SOUS-DOMAINE.ngrok-free.app/api/v1';
